import { Transform, Writable, type TransformCallback, type WritableOptions } from 'node:stream';
/**
 * Extracts individual JPEG frames out of a multipart MJPEG-over-HTTP byte stream by
 * scanning for JPEG start/end-of-image markers and the multipart `Content-Length` header.
 */
export declare class MjpegFrameParser extends Transform {
    private buffer;
    private expectedLength;
    private bytesWritten;
    private isReading;
    _transform(chunk: Buffer, _encoding: BufferEncoding, callback: TransformCallback): void;
    private startFrame;
    private appendChunk;
    private emitFrame;
}
/**
 * Connects to an MJPEG-over-HTTP stream and keeps track of the last JPEG frame received,
 * so that it can be used as a cheap, low-latency screenshot source.
 */
export declare class MJpegStream extends Writable {
    readonly errorHandler: (err: Error) => void;
    readonly url: string;
    private updateCount;
    private lastChunk;
    private registerStartSuccess;
    private registerStartFailure;
    private responseStream;
    private consumer;
    /**
     * @param mJpegUrl - URL of the MJPEG-over-HTTP stream
     * @param errorHandler - additional function that will be called in the case of any errors
     * @param options - Options to pass to the Writable constructor
     */
    constructor(mJpegUrl: string, errorHandler?: (err: Error) => void, options?: WritableOptions);
    get lastChunkBase64(): string | null;
    lastChunkPNG(): Promise<Buffer | null>;
    lastChunkPNGBase64(): Promise<string | null>;
    clear(): void;
    start(serverTimeout?: number): Promise<void>;
    stop(): void;
    _write(chunk: Buffer | string, _encoding: BufferEncoding, callback: (error?: Error | null) => void): void;
}
//# sourceMappingURL=mjpeg.d.ts.map